import { makeStyles } from '@material-ui/core/styles';
export default makeStyles({
  root: {
    maxWidth: 345,
    borderRadius: '100%',
  },
  cardMedia: {
    width: 200,
    height: 200,
    '&:hover': {},
  },
});
